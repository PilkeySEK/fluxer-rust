# neptunium-http

A crate for interacting with the Fluxer HTTP API (REST API). This is used by fluxer-neptunium, but can also be used individually, if you require fine-grained control over what is sent and received.