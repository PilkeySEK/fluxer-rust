# neptunium-model

This crate has all types relevant to the Fluxer gateway, as well as many types relevant to the Fluxer HTTP API (REST API). It is used by fluxer-neptunium, but can also be used individually, if you require just the types.